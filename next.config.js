/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true, // For pixel art GIFs/SVGs
  },
  // Uncomment for static export
  // output: 'export',
};

module.exports = nextConfig;

